name: race_word_game
description: 单词竞速卡片 - 儿童英语学习应用
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.1.0
  flutter_tts: ^4.2.0
  shared_preferences: ^2.3.0
  provider: ^6.1.0
  path_provider: ^2.1.0
  uuid: ^4.5.1
  cached_network_image: ^3.4.0
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.0
  share_plus: ^7.2.0
  speech_to_text: ^6.6.0
  ar_flutter_plugin: ^0.7.0
  # charts_flutter: ^0.12.0  # Temporarily disabled - package maintenance issue
  intl: ^0.19.0
  connectivity_plus: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.0
  build_runner: ^2.4.0
  network_image_mock: ^2.1.1

flutter:
  uses-material-design: true
